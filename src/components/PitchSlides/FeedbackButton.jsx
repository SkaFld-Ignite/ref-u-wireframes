import React from 'react';

const FeedbackButton = ({ slideNumber, slideTitle }) => {
  // Hidden for now - feedback system requires GitHub login which isn't ideal UX
  return null;
};

export default FeedbackButton;
